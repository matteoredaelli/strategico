select 
  item_id id,
  <%=paste(project.keys, collapse=", ")%>,
  BestModel,
  SuggestedModel,
  SuggestedModel2
FROM 
  v_<%=project.name%>_summary_<%=value%>
WHERE
  BestModel != SuggestedModel
order by item_id
